type: 'automation'

project:
  name: 'EKS Scaler'
  description: 'Module to scale EKS clusters based on schedules and manual triggers'
  channel: '#eks-scaler'
  owner: 'thomas'
  maintainers: ['thomas']

tasks:
  install:
    command: 'yarn install'
    inputs: 
      - 'package.json'
      - 'package-lock.json'
    outputs:
      - 'node_modules'